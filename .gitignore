node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Next.js build output
.next/

# SQLite database (local only)
prisma/dev.db
prisma/dev.db-journal

# Local data files (LSN imports — keep private)
data/*.xls
data/*.xlsx
data/*.csv
# But DO track program config
!data/program.json

# OS files
.DS_Store
